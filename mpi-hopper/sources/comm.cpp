#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "comm.h"

/* Divide and send/recieve the matrix before calculation */
void spreadMatrix(/* PARAMETERS */) {

}

/* Gather the finished result to P(0,0,0) */
void gatherMatrix(/* PARAMETERS */) {

}

/* Splits the world comm into cartesian comms */
int commsSplit(/* PARAMETERS */) {

}
